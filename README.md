# P_Bulles-JS
Créer un site internet sur un hobby en JS. Respecter une structure MVC. Utiliser Node.js, Express, et EJS.
Stocker les images dans une DB.

Commandes docker:
    docker compose down -v
    docker compose up --build

Dans un autre cmd
    docker compose exec db mariadb -usolart -psolartpass -D solart